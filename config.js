// config.js
// ─────────────────────────────────────────────────────────────────────────────
// Design / layout knobs — tweak these and refresh the page to play with the look.
// All values are plain CSS lengths (e.g. "1rem", "16px", "0.5rem", "0.75rem").
// These override the default Tailwind spacing classes in index.html.
// ─────────────────────────────────────────────────────────────────────────────
const theme = {
    layout: {
        sectionSpacing: "2rem",     // vertical space below each section (gap between sections)
        headingSpacing: "0.75rem",  // gap between a section heading (the underline) and its content
        tagSpacing: "0.75rem",      // gap between the Interests tags
    },
};
